<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://android.com"
    xmlns:tools="http://android.com">

    <!-- आवश्यक परमिशन जो नए एंड्रॉइड वर्शन्स के लिए ज़रूरी हैं -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/my_logo"
        android:label="Only Fair Hacks"
        android:roundIcon="@drawable/my_logo"
        android:supportsRtl="true">

        <activity
            android:name="com.android.support.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Android 14+ क्रैश से बचने के लिए演出 सर्विस टाइप और प्रॉपर्टी -->
        <service
            android:name="com.android.support.Launcher"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:node="merge">
            
            <property 
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating Mod Menu for developer testing and UI experimentation" />
        </service>

    </application>
</manifest>
